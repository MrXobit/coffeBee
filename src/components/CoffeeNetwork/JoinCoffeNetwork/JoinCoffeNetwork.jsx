import React from 'react'

const JoinCoffeNetwork = ({setChoice}) => {
  return (
    <div>
      join coffe network
      <button onClick={() => (setChoice(1))}>main page</button>
    </div>
  )
}

export default JoinCoffeNetwork
